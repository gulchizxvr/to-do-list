import styled from "styled-components"

export const StyledHomePage = styled.div`
    font-family: "Arial", serif;
    font-size: 1rem;
    padding: 10px 20px 20px;
    background-color: rgba(211, 210, 210, 0.33);
    min-height: 100vh;
    box-sizing: border-box;
    cursor: default;

    h1 {
        margin: 0 auto 10px;
    }
`